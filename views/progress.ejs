<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta content="width=device-width,initial-scale=1" name="viewport">
        <title><%= courseTitle %> Progress</title>
        <link href="./courses/courses.css" rel="stylesheet">
        <link href="./main.css" rel="stylesheet">
        <script src="./sketch.js"></script>
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.0/jquery.min.js"></script>
    </head>

    <body class="side">
        <div class="reset-button"><button class="button small-button hover-anim" onclick="course.reset()">Reset</button></div>
        <% for( let currentChapter = 0; currentChapter < chapters.length; currentChapter++ ) { %>
            <div class="chapter <%= currentChapter+1 %>"><h1>Chapter <%= currentChapter %>: <%= chapters[currentChapter].chapter_title %></h1></div>
        <% } %>
    </body>
</html>